server:
  servlet:
    context-path:
  port:

spring:
  datasource:
    url:
    username:
    password:
  jpa:
    properties:
      hibernate:
        dialect:
    hibernate:
      ddl-auto:

tracto:
  download-schedule:
    url:
    auth-header:
    exam-period-url:
  time-update-schedule:
  time-update-exam-period:
  save-csv-url:

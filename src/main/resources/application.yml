server:
  servlet:
    context-path:
  port:

spring:
  datasource:
    url:
    username:
    password:
    driver-class-name:
  jpa:
    properties:
      hibernate:
        dialect:
    hibernate:
      ddl-auto:

tracto:
  download-schedule:
    url:
    auth-header:
    exam-period-url:
    extramural-url:
  time-update-schedule:
  time-update-exam-period:
  time-update-extramural:
